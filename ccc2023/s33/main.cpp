#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);

    int n, m, r, c;
    cin >> n >> m >> r >> c;
    char poster[n][m]; memset(poster, 0, sizeof(poster));
    for(int i = 0; i < n; i++){
        for(int j = 0; j < m; j++){
            while()
        }
    }

    return 0;
}
