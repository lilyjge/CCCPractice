#include <bits/stdc++.h>

using namespace std;
typedef pair<int, int> pi;
int h, k;
vector<int> houses;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> h;
    for(int i = 0; i < h; i++){
        int a; cin >> a;
        houses.push_back(a);
    }
    cin >> k;
    

    return 0;
}